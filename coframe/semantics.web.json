{
  "version": "1.0.0",
  "extends": "coframe/semantics.base.json#1.0.0",
  "keys": {
    "ui.background": {
      "desc": "Background visual treatment for UI surfaces.",
      "protocol": {
        "preconditions": [],
        "actions": [],
        "postconditions": [
          "ASSERT: contrast meets AA on text overlays",
          "ASSERT: performance budget respected (no large, blocking assets)"
        ],
        "failure_policy": "continue_with_warning"
      }
    },
    "ui.theme": {
      "desc": "Design system/theme identity applied across the SPA.",
      "protocol": {
        "preconditions": [],
        "actions": [],
        "postconditions": [
          "ASSERT: consistent tokens (colors/spacing/typography)",
          "ASSERT: shared components match theme spec"
        ],
        "failure_policy": "continue_with_warning"
      }
    }
  },
  "operators": {
    "op.ui.starry": {
      "desc": "Apply a starry night background theme.",
      "assumptions": [
        "Theme assets present under public/ or apps/*/public"
      ],
      "steps": [
        "Add background class or CSS vars",
        "Reference star field asset or CSS gradient",
        "Verify contrast and scroll performance"
      ]
    }
    ,
    "op.ui.theme.the_choosen_one": {
      "desc": "Apply the 'the-choosen-one' theme (shared look & feel).",
      "assumptions": [
        "Shared CSS variables and component styles are present",
        "Brand assets (logos/icons) available under public/"
      ],
      "steps": [
        "Import theme CSS variables (e.g., :root tokens)",
        "Apply base layout and spacing scale",
        "Ensure buttons/forms/nav use the themed component styles",
        "Verify dark/light contrast per accessibility guidance"
      ]
    }
  },
  "merge": {
    "hints": {
      "notes": "append"
    }
  }
}
